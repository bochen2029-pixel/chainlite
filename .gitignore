# build outputs
bin/
*.obj
*.exe
*.pdb
*.ilk
*.exp
*.lib

# per-node runtime state — includes PRIVATE KEYS (node.key) and the chain databases
data/
logs/
chainlite-data/

# generated at build time from web/viewer.html by tools/embed.ps1
src/viewer_html.h

# private keys / wallets — never commit
*.key

# local demo artifacts
proof*.txt
mydoc.txt

# editor / OS cruft
.vs/
.vscode/
*.user
Thumbs.db
